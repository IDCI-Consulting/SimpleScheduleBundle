{
    "id": "{{ entity.id }}",
    "name": "{{ entity.name }}",
    "slug": "{{ entity.slug }}",
    "color": "{{ entity.color }}",
    "description": "{% autoescape 'js' %}{{ entity.description }}{% endautoescape %}",
    "level": "{{ entity.level }}",
    "tree": "{{ entity.tree }}"
}