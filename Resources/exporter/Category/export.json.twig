{% autoescape false %}
{
    "id": "{{ entity.id }}",
    "name": "{{ entity.name }}",
    "slug": "{{ entity.slug }}",
    "color": "{{ entity.color }}",
    "description": "{{ entity.description }}",
    "level": "{{ entity.level }}",
    "tree": "{{ entity.tree }}"
}
{% endautoescape %}
